#include <stdio.h>
#include <stdlib.h>
#define MAX 10000005
int arr[MAX];

int main(void) {
  /* code */
  int i,j,n,k,t;
  scanf("%d",&t);
  while(t--){
    scanf("%d%d",&n,&k);
    for(i=0;i<n;i++)
    {
      scanf("%d",&arr[i]);
    }
    
  }
  return 0;
}
